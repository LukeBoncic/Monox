#ifndef LIBRARY_H
#define LIBRARY_H

void memset(void *buffer, char value, int size);
void memcpy(void* destination, void* source, int size);
int memcmp(void* address1, void* address2, int size);

#endif
